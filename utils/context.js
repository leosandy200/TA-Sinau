import { createContext } from "react";

export const ProfileContext = createContext(null);
export const BelajarContext = createContext(null);
export const AchievementContext = createContext(null);
export const FollowContext = createContext(null);
export const ProfileDataContext = createContext(null);
export const PembelajaranContext = createContext(null);

